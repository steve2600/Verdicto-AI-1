import os

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 8001))  # Use Railway's PORT or default to 8001
    uvicorn.run(
        "api:app",
        host="0.0.0.0",
        port=port,
        reload=True,
        log_level="info"
    )